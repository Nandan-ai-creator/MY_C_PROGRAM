#include<stdio.h>
int main()
{
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    if(num>0)
        printf("positive number\n",&num);
        else
        printf("negative number\n",&num);
}
